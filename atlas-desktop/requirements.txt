# Atlas Desktop — hands-free voice client (macOS)
# Python 3.11+ recommended.

# --- The brain: run the Atlas Hub project as a persistent agent ---
# Uses your Claude Code login (no ANTHROPIC_API_KEY). Needs the `claude` CLI installed.
claude-agent-sdk>=0.2.110      # Claude Agent SDK (Python). Formerly claude-code-sdk.

# --- Wake word: "Hey Atlas" (local, on-device) ---
pvporcupine>=3.0.0             # Picovoice Porcupine wake-word engine
pvrecorder>=1.2.0             # simple cross-platform mic recorder for the spotter

# --- Voice activity detection / endpointing ---
onnxruntime>=1.17.0            # runtime for Silero VAD
silero-vad>=5.1               # robust speech endpointing (knows when you've stopped)

# --- Voice: Gemini STT + TTS (default; key-only) ---
google-genai>=1.0.0           # Gemini API SDK (speech-to-text + text-to-speech)

# --- Voice: Google Cloud "Chirp" (OPTIONAL — only if provider=google-chirp) ---
# Uncomment if you set up a Google Cloud project with Speech-to-Text + Text-to-Speech.
# google-cloud-speech>=2.26.0        # STT — Chirp 2 (v2 API)
# google-cloud-texttospeech>=2.16.0  # TTS — Chirp 3: HD voices

# --- Audio I/O ---
sounddevice>=0.4.6            # low-latency mic capture + speaker playback (PortAudio)
numpy>=1.26.0

# --- Local HUD (optional but on by default) ---
websockets>=12.0             # push conversation state to the orb
aiohttp>=3.9.0               # serve the HUD page on localhost

# --- Config / utils ---
pyyaml>=6.0
python-dotenv>=1.0.0
